# Core pipeline
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# Power BI workbook export
openpyxl>=3.1

"""
Optional / legacy — only needed if you use fetch_data.py to scrape via soccerdata 
instead of the Kaggle CSVs. Not required for the pipeline as documented in the README.

soccerdata

"""
